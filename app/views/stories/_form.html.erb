<div class="centerform">
  <%= form_with(model: story) do |form| %>
  <% if story.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(story.errors.count, "error") %> prohibited this story from being saved:</h2>

      <ul>
        <% story.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form">
    <div class="input-container ic">
      <%= form.text_field :title, class:"input", placeholder: " " %>
       <div class="cut"></div>
      <%= form.label :title, class:"placeholder" %>
    </div>
      <div class="input-container ic">
        <%= form.text_field :duration, class:"input", placeholder: " " %>
         <div class="cut"></div>
        <%= form.label :duration, class:"placeholder" %>
      </div>

      <div class="input-container ic">
        <%= form.number_field :weight, class:"input", placeholder: " " %>
         <div class="cut"></div>
        <%= form.label :weight, class:"placeholder" %>
      </div>

      <div>
        <%= form.submit class:"submit" %>
      </div>
    </div>
  </div>
  <% end %>
</div>